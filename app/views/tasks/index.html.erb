<h1>My tasks</h1>
<ul>
<% @tasks.each do |t| %>
  <li>
    <%= t.completed ? '[x] ' : '[ ] ' %>
    <%= link_to t.title, task_path(t) %>
  </li>
<% end %>
</ul>
<hr>
<%= link_to "Add a new task", create_task_path %>
