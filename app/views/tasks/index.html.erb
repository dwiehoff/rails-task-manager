<h1>My tasks</h1>
<ul>
<% @tasks.each do |t| %>
  <li>
    <%= t.completed ? '[x] ' : '[ ] ' %>
    <%= link_to t.title, task_path(t) %>
  </li>
<% end %>
</ul>
<hr>
<%= link_to "Add a new task", new_task_path %>
